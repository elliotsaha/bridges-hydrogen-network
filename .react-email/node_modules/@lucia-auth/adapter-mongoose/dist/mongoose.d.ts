import type { Adapter, InitializeAdapter, KeySchema, SessionSchema, UserSchema } from "lucia";
import type { Model } from "mongoose";
import type { KeyDoc, SessionDoc, UserDoc } from "./docs.js";
export declare const DEFAULT_PROJECTION: {
    $__: number;
    __v: number;
    _doc: number;
};
export declare const mongooseAdapter: (models: {
    User: Model<UserDoc>;
    Session: Model<SessionDoc> | null;
    Key: Model<KeyDoc>;
}) => InitializeAdapter<Adapter>;
export declare const createMongoValues: (object: Record<any, any>) => {
    [k: string]: any;
};
export declare const transformUserDoc: (row: UserDoc) => UserSchema;
export declare const transformSessionDoc: (row: SessionDoc) => SessionSchema;
export declare const transformKeyDoc: (row: KeyDoc) => KeySchema;
